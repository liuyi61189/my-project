# 2026-07-15

## 生成用例列表三级联动筛选（2026-07-15）
- 用户需求：选了版本后，功能模块只显示该版本下的；选了功能模块后，测试点只显示该模块下的
- **改动**：`frontend/src/views/requirement-analysis/GeneratedTestCaseList.vue`
  - 修复 `filteredFeatureModules` computed：优先按版本所属项目过滤（之前只按项目过滤，版本选择无效）
  - 新增 `filteredTestPoints` computed：按选中功能模块过滤测试点
  - 新增 `selectedTestPoint` 状态 + `allTestPoints` 数据
  - 新增 `onFeatureModuleChange()` 方法：切模块时清空测试点 + 加载该模块测试点
  - 新增 `fetchAllTestPoints()` / `fetchTestPoints(moduleId)` 方法
  - 模板新增「测试点」下拉 select
  - 联动重置：切项目→清版本+模块+测试点；切版本→清模块+测试点；切模块→清测试点
  - `loadTasks` 和统计请求已自带 test_point 参数支持
- 部署：npm run build + docker cp dist + nginx reload

## 版本管理弹窗模块过滤修复（2026-07-15 下午）
- **问题**：版本管理"管理模块和测试点"弹窗显示所有项目模块，不区分版本
- **根因**：`FeatureModule` 模型只有 `project` FK，无 `version` 关联；前端按 version.projects 查模块，后端只按 project 过滤
- **修复**：
  - `FeatureModule` 新增 `versions = M2M(Version)` 字段
  - MySQL 建 `feature_modules_versions` 中间表（raw SQL 幂等）
  - 后端 `get_project_feature_modules` + `FeatureModuleListCreateView` 支持 `?version_id=` 过滤
  - 序列化器加 `version_ids`(写入) + `versions`(读取) 字段；创建/编辑时关联版本
  - 前端 `VersionList.vue`：请求带 `version_id` 参数；创建模块自动关联当前版本
  - 已有数据回填：按 project→version 关系 INSERT IGNORE
- **后续补充（用户反馈"怎么改"）**：编辑/新增模块弹窗增加「关联版本」多选框(checkbox-group)，用户可手动分配模块到多个版本或从某版本移除。序列化器新增 `get_versions()` 返回 `{id, name}[]`
- 部署：docker cp 后端3文件 + restart + npm run build + docker cp dist + nginx reload

## 模块弹窗「关联版本」按项目过滤 + 保存时尊重用户选择（2026-07-15）
- **问题1**：编辑模块弹窗中「关联版本」复选框显示了所有项目的版本，应只显示当前项目关联的版本
  - **修复**：`fetchAllVersionsForModule()` 改为获取全部版本后，按 `manageVersion.projects` 的 project_id 过滤，只保留同项目下的版本
- **问题2**：`saveModule()` 中 `version_ids` 被硬编码为 `[manageVersion.value.id]`，忽略用户复选框选择
  - **修复**：改为 `version_ids: moduleForm.version_ids`，完全尊重用户勾选
- **测试用例端确认已完备**：TestCase 模型有 `versions` M2M；Create/Update 序列化器有 `version_ids`；TestCaseList 有版本筛选下拉；TestCaseForm/TestCaseEdit 有版本多选框——均按项目联动，无需额外改动
- 部署：npm run build + docker cp dist + nginx reload

## 测试用例列表「功能模块」筛选按版本关联过滤（2026-07-15）
- **问题**：测试用例列表选了版本 V2.0.0 后，功能模块下拉仍显示该项目下所有模块，未按版本关联过滤
- **根因**：`filteredFeatureModules` computed 只按 version.projects 的 project_id 过滤（即只限制到同项目），未检查模块的 `versions` 字段是否包含该版本
- **修复**：加双重过滤——模块的 `fm.versions` 必须包含当前版本 ID（`v.id === vid`），同时仍在版本所属项目范围内
- 部署：npm run build + docker cp dist + nginx reload

## 测试用例功能模块+测试点关联恢复（2026-07-15）
- **问题**：昨天删除测试点导致轻卡日常(project=4)的 405 条用例中 287 条丢失了 `test_point_id` 和 `feature_modules` M2M 关联
- **方案**：按用例标题关键词匹配到 6 个现有模块（断食/ar拍照/美食贴纸/减肥人格测试/围度/食谱），每模块对应 1 个测试点
- **执行**：5 轮关键词扩展匹配，最终 405/405 全部恢复（100%），各模块分布：断食172、ar拍照93、美食贴纸64、减肥人格54、围度21、食谱20
- **注意**：这是纯数据修复，无代码变更

## AI 用例生成失败排查（2026-07-14 晚）
- 任务 TASK_4ED1C8CD 失败原因：**硅基流动 API 余额不足**（403: "account balance is insufficient"）
- 非代码问题，需充值硅基流动账户后重试
