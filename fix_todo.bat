@echo off
docker exec testhub-backend python -c "import os; os.remove('/app/apps/ui_automation/knowledge/element_knowledge.json') if os.path.exists('/app/apps/ui_automation/knowledge/element_knowledge.json') else None; import django; os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'backend.settings'); django.setup(); from apps.ui_automation.smart_locator import _load_kb; kb=_load_kb(); print('KB elements:', len(kb['万年历']['elements'])); from apps.ui_automation.models import Element; from apps.ui_automation.smart_locator import smart_match_locator; fixed=0; [print(el.name, '->', (r:=smart_match_locator(el.name))) or (setattr(el,'locator_value',r[1]) or el.save() or [fixed:=fixed+1]) if r and r[1] else None for el in Element.objects.filter(locator_value='TODO_待补充控件定位器')]; print('Fixed', fixed, 'elements')"
